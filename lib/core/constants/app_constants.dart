/// Global app-level constants.
/// Replace placeholder values before release.
abstract final class AppConstants {
  static const String appName = 'App Name'; // TODO: update before release
  static const String bundleId = 'com.placeholder.appname'; // TODO: update before release

  /// API
  static const String baseUrl = 'https://api.example.com'; // TODO: update
  static const Duration connectTimeout = Duration(seconds: 30);
  static const Duration receiveTimeout = Duration(seconds: 30);
}
